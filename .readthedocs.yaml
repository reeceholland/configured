version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  apt_packages:
    - doxygen
    - graphviz
  jobs:
    pre_build:
      - doxygen docs/Doxyfile.readthedocs

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt
